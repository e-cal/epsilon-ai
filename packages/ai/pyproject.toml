[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "e-ai"
version = "0.1.0"
description = "Epsilon AI router and provider core"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28.1"]

[tool.hatch.build.targets.wheel]
packages = ["e_ai"]

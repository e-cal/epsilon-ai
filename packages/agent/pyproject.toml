[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "e-agent"
version = "0.1.0"
description = "Epsilon agent framework"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["e-ai"]

[tool.hatch.build.targets.wheel]
packages = ["src/e_agent"]

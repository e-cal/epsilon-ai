# `epsai.agent`

Epsilon AI Framework module corresponding to the upstream `pi-mono/packages/agent` package.

Planned scope:

- agent state and events
- agent loop and continuation semantics
- tool execution pipeline
- steering and follow-up queues
